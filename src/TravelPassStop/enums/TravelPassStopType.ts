const TYPE_PICKUP = 'STOP_TYPE_PICKUP';
const TYPE_BOTH = 'STOP_TYPE_PICKUP_AND_DROPOFF';
const TYPE_DROPOFF = 'STOP_TYPE_DROPOFF';

enum TravelPassStopType {
  TYPE_PICKUP,
  TYPE_BOTH,
  TYPE_DROPOFF
}

export default TravelPassStopType;
